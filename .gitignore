# Python
venv/
__pycache__/
*.pyc

# ความลับ/ค่าประจำเครื่อง — ห้ามเข้า repo (ใช้ .env.example เป็นแม่แบบ)
.env

# แคชแผนที่ออฟไลน์ (เว็บโหลดใหม่เองได้)
fdc/tile_cache/

# รูปถ่าย/เอกสารที่ root — เก็บในเครื่อง ไม่เข้า repo (ตัดสินใจ 2026-07-20)
/*.jpg
/*.pdf
